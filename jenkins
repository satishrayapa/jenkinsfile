def var1 = choice(name: 'CHOICE', choices: ['master', 'node25'], description: 'Pick something')

pipeline {
    agent { label "${params.var1}" }
    stages {
        stage ('tag') {
            steps {
	        echo 'hello world'
                git 'https://github.com/satishrayapa/devops7.git'
            }
            
        }
    }
}
